apiVersion: v1
kind: Namespace
metadata:
  name: rookpp-system
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: rookpp-controller
  namespace: rookpp-system
  labels:
    app: rookpp
spec:
  replicas: 1
  selector:
    matchLabels:
      app: rookpp
  template:
    metadata:
      labels:
        app: rookpp
    spec:
      serviceAccountName: rookpp
      containers:
        - name: manager
          image: ghcr.io/jackal/rookpp:latest
          args:
            - --leader-elect
          ports:
            - containerPort: 8080
              name: metrics
            - containerPort: 8081
              name: health
          livenessProbe:
            httpGet:
              path: /healthz
              port: 8081
            initialDelaySeconds: 15
          readinessProbe:
            httpGet:
              path: /readyz
              port: 8081
            initialDelaySeconds: 5
          resources:
            requests:
              cpu: 50m
              memory: 128Mi
            limits:
              memory: 256Mi
---
apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: rookpp-disk-scanner
  namespace: rookpp-system
  labels:
    app: rookpp-disk-scanner
spec:
  selector:
    matchLabels:
      app: rookpp-disk-scanner
  template:
    metadata:
      labels:
        app: rookpp-disk-scanner
    spec:
      serviceAccountName: rookpp
      hostPID: true
      tolerations:
        - operator: Exists
      containers:
        - name: agent
          image: ghcr.io/jackal/rookpp-agent:latest
          securityContext:
            privileged: true
          env:
            - name: NODE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: spec.nodeName
          args:
            - --interval=60s
          volumeMounts:
            - name: dev
              mountPath: /dev
            - name: sys
              mountPath: /sys
      volumes:
        - name: dev
          hostPath:
            path: /dev
        - name: sys
          hostPath:
            path: /sys
